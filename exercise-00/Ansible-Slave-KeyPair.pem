    1  sudo su -
    2  ls
    3  sudo su -i
    4  sudo su -
    5  mkdir vprofile-practice
    6  cd vprofile-practice/
    7  mkdir exercise-01
    8  vim inventory 
    9  vim Ansible-Slave-KeyPair.pem  (Copy the whole pem file from the local disk where ever you stored)
   10  ansible web01 -m ping -i inventory
   11 After the above command click on no
   with the root user 
    1  exit
    2  cd /etc/ansible/
    3  ls
    4  mv ansible.cfg ansible.cfg-backup
    5  ls
    6  ansible-config init --disabled -t all > ansible.cfg
    7  ls
    8  vim ansible.cfg
      search for /host_key(if you that click on Enter to go there) make this as False

     final: ansible web01 -m ping -i inventory 